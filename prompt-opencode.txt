Do these tasks in order on the repo at F:\Coding\instrumation. Make git commits after each task with the specified message.

### Task 1: Issue #82 - Add set_ovp/set_ocp/clear_protection print stubs to SimulatedPowerSupply

File: src/instrumation/drivers/simulated.py

In the `SimulatedPowerSupply` class (around line 98), these three methods are currently bare `pass` stubs:

```python
def set_ovp(self, voltage: float): pass
def set_ocp(self, current: float): pass
def clear_protection(self): pass
```

Replace each `pass` with a `print()` call that logs the action, consistent with how `set_voltage` and `set_output` work in the same class:

```python
def set_ovp(self, voltage: float):
    print(f"[SIM] PSU OVP: {voltage} V")
def set_ocp(self, current: float):
    print(f"[SIM] PSU OCP: {current} A")
def clear_protection(self):
    print("[SIM] PSU Protection Cleared")
```

Commit message: "fix: add print stubs for set_ovp/set_ocp/clear_protection on SimulatedPowerSupply (#82)"

### Task 2: Issue #83 - Make measure_power on SimulatedPowerSupply return realistic value

File: src/instrumation/drivers/simulated.py

In `SimulatedPowerSupply`, change `measure_power` from returning `MeasurementResult(0.0, "W")` to computing a realistic value using the stored `_voltage`:

```python
def measure_power(self) -> MeasurementResult:
    return MeasurementResult(self._voltage * 0.5, "W")
```

Note: `_voltage` is set by `set_voltage(self, voltage)` on line 87: `self._voltage = voltage`. So if voltage was set to 5V, power = 5 * 0.5 = 2.5W.

Commit message: "fix: make SimulatedPowerSupply.measure_power return voltage * 0.5 (#83)"

### Task 3: Issue #84 - Add set_trigger print stub to SimulatedOscilloscope

File: src/instrumation/drivers/simulated.py

In `SimulatedOscilloscope`, `set_trigger` is currently:
```python
def set_trigger(self, source, level, slope): pass
```

Replace with:
```python
def set_trigger(self, source, level, slope):
    print(f"[SIM] Scope Trigger: source={source}, level={level}, slope={slope}")
```

Consistent with how `run`, `stop`, `single`, and `auto_scale` work.

Commit message: "fix: add set_trigger print stub to SimulatedOscilloscope (#84)"

### Task 4: Issue #85 - Add peak_search/get_marker_x/get_marker_y print stubs to SimulatedNetworkAnalyzer

File: src/instrumation/drivers/simulated.py

In `SimulatedNetworkAnalyzer`, these three methods need print() calls added:

```python
def peak_search(self, marker: int = 1): pass
def get_marker_x(self, marker: int = 1) -> float: return 2.4e9
def get_marker_y(self, marker: int = 1) -> float: return -10.0
```

Replace with:
```python
def peak_search(self, marker: int = 1):
    print(f"[SIM] VNA Peak Search: marker {marker}")
def get_marker_x(self, marker: int = 1) -> float:
    print(f"[SIM] VNA Get Marker X: marker {marker}")
    return 2.4e9
def get_marker_y(self, marker: int = 1) -> float:
    print(f"[SIM] VNA Get Marker Y: marker {marker}")
    return -10.0
```

Commit message: "fix: add peak_search/get_marker_x/get_marker_y print stubs to SimulatedNetworkAnalyzer (#85)"
